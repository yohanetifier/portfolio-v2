'use client';
import Plane from '@/components/Plane/Plane';
import { Canvas } from '@react-three/fiber';

export default function TestPage() {
  return (
    <Canvas className="mt-20 border-2 border-red-500 w-full h-screen">
      {/* <Plane /> */}
    </Canvas>
  );
}
